# EAS Build will NOT ignore these files even if they're in .gitignore
# We'll generate these from environment variables during build
!android/app/google-services.json
!ios/GoogleService-Info.plist

# Ensure root-level Firebase config files are included (used by managed expo config)
!google-services.json
!GoogleService-Info.plist
